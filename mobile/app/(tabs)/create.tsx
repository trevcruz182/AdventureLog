import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function CreateScreen() {
    return (
        <ScreenPlaceholder 
            eyebrow="New Memory"
            title="Log an adventure."
            description="Capture the place, photos, details, and moments that made the experience yours."
        />
    );
}